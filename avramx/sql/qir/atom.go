package qir

const (
	LAMBDA  Atom = "lambda"
	SCAN    Atom = "scan"
	SELECT  Atom = "select"
	PROJECT Atom = "project"
	SORT    Atom = "sort"
	LIMIT   Atom = "limit"
	GROUP   Atom = "group"
	JOIN    Atom = "join"
)
